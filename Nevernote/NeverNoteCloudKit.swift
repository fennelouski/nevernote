import Foundation

enum NevernoteCloudKit {
    /// Must match the CloudKit container enabled for both iOS and watchOS targets (and in the Apple Developer portal).
    static let containerIdentifier = "iCloud.com.nathanfennel.NeverNote"
}
